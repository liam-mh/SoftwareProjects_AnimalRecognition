<!DOCTYPE html>
<html lan="en" and dir="Itr">
<head>
    <meta charset="utf-8">
    <title> Interactive Login Form</title>
    <link rel="stylesheet" href="results.css">
    <script src ="admin-login.js"> </script>
</head>

<body>

  <!--- nav bar --->
  
    <header>
      <nav class="navbar">
          <div class="navbar__container">
            <a href="/" id="navbar__logo"><i class="fas fa-gem"></i><img src="frontEndImages/elanco.png" alt="Logo" /></a>
            <div class="navbar__toggle" id="mobile-menu">
              <span class="bar"></span> <span class="bar"></span>
              <span class="bar"></span>
            </div>
            <ul class="navbar__menu">
              <li class="navbar__item">
                <a href="/" class="navbar__links">Home</a>
              </li>
              <li class="navbar__item">
                <a href="/tech.html" class="navbar__links">Tech</a>
              </li>
              <li class="navbar__item">
                <a href="/" class="navbar__links">API</a>
              </li>
              <li class="navbar__item"><a href="/login" class="navbar__links">Sign Up</a></li>
            </ul>
          </div>
        </nav>
  </header>

  <!-- body of results page -->

  <h1>Results</h1>

  <% for (let image of images) { %>
    <% var path = './userImages/' + image.path %>

    <div class="main">
      <div class="main__container">

        <!-- left side -->
        <div class="main__content">

          <% for (let label of image.labels) { %>
            <li>
              <div class="column">
                <div class="row">
                  <%= label.description %>
                </div>
                <div class="row">
                  Confidence: 
                  <% if (label.score > 0.9) { %>
                    <span style="color: green">High</span>
                  <% } else if (label.score > 0.7) { %> 
                    <span style="color: yellow">Medium</span>
                  <% } else { %> 
                    <span style="color: red">Low</span>
                  <% } %>
                </div>
              </div>  
            </li>
          <% } %>
        
        </div>

        <!-- right side -->
        <div class="main__img--container">
          
          <img id="main__img" src="<%= path %>" alt="pic" id="main__img" width="300" height="300">
        
        </div>
      </div>
    </div>
    <br>
  <% } %>
   
  
    

</body>
</html>